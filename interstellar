let circleX = 100;

function setup() {
  createCanvas(400, 300);
  
}

function mousePressed() {
  circleX = -23;
}

function draw() {
  background(0);
  stroke(150, 500, 76);
  fill(255, 30, 100);
  circle(circleX, 150, 20);
  circleX = circleX + 1;
  
  
}
